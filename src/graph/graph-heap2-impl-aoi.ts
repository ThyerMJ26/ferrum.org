
// TODO An implementation of the Heap interface using an ArrayBuffer and small integers.
// TODO The V8 JIT should work well on function which only use in Int32Array and small integers.
// TODO A SharedArrayBuffer can be shared between threads/workers.
// TODO   This would mean graph-reductions could occur concurrently with UI interactions.
// TODO 

import { assert } from "../utils/assert.js"
import { Heap } from "./graph-heap2.js"

// Array of Ints
export function mkHeap_AoI(): Heap {
    assert.todo()
}


